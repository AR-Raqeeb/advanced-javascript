// double equal (==) checks only value, it tries to convert type to match value
// triple equal (===) checks both value and type

const first = 1;
const second = true;
if(first === second){
    console.log("condition is true");
}
else{
    console.log("condition is false");
}